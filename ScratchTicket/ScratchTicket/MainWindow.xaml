<Window x:Class="ScratchTicket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScratchTicket"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen" 
        Closed="Window_Closed"
        Title="MainWindow" Height="800" Width="1200">
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="698*"/>
            <RowDefinition Height="87*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center">
            <Image Source="Resources/金币2.png" Width="64" Height="64" Margin="20 0 4 0"/>
            <TextBlock x:Name="tbAssets" Text="100" FontSize="32" FontWeight="Bold" Foreground="DarkOrange" VerticalAlignment="Center" Margin="0 0 10 0" />
            <Canvas Width="400" Height="{Binding ElementName=tbAssets,Path=ActualHeight}">
                <TextBlock Text="+100" Canvas.Left="0" Canvas.Top="0" FontSize="32" Foreground="LightGreen" />
            </Canvas>
        </StackPanel>
    </Grid>
</Window>
